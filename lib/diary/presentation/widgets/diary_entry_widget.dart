import 'package:flutter/material.dart';

class DiaryEntryWidget extends StatelessWidget {
  const DiaryEntryWidget({Key key?}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
